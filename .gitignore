.DS_Store
node_modules/
.build/
.swiftpm/

# Yarn — the node-modules linker keeps only local state here; yarn.lock stays tracked
.yarn/*
!.yarn/patches
.pnp.*

# wordlist-filter output, regenerated per run
filter-output/

# compiled .lexdi artifacts, rebuilt by the publish pipeline and never committed
dist/
